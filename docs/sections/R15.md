### Stream Overview & Mathematical Target
R15 regenerates v87 Figure 17 (`fig:cross_section`) and L376 by running the cross-sectional sign-CUSUM monitor on 97 surviving US equities, 2005–2025 (5154 trading days). It pools K correlated streams under two calibrations—an independence assumption and a bootstrap over real null windows—and races the pooled monitor against single streams under injected drift. The experiment also discharges L389 by providing a public equity fetcher for the 97-equity panel.

### Execution & Verification
Execution enforces strict determinism via `enforce_strict_determinism()` with 128-bit SeedSequence re-keying. The frozen composition `assets_idx` is carried verbatim, while all Monte-Carlo draws migrate to integer-grid-indexed keys. Both default and `--witness-blas` attribution arms run unconditionally; the latter isolates `MKL_CBWR=COMPATIBLE` as the sole environment difference, recovering submitted values bit-for-bit on all four RNG-free columns (`rho_sign_meas`, `K_eff_meas`, `K_eff_ana`, `ljungbox_p_Pt`) across all ten K.

### Output Artifacts & Deviation Classification
Artifacts include `R15_panel_diagnostics.csv` (10 rows), `R15_cross_sectional_race.csv` (50 rows), `R15_covid_natural.csv` (10 rows), `R15_panel_composition.csv` (388 rows), `R15_race_windows.csv` (110000 rows), `R15_scatter_correlation.csv` (5 rows), figure `fig17_cross_section.png`, and LaTeX macros `R15_claims.tex` with 15 `\newcommand` definitions under `\RFifteen*`. Six of eight v87 numerals classify D0/D1; two caption quantities move: bootstrap FPR envelope `4.8–6.4%` → `4.0–5.9%` (R15-campaign-redraw, D2) and caption correlation `r ≥ 0.99` → `|r| ≈ 0.99` with negative sign (R15-scatter-sign, D2).
