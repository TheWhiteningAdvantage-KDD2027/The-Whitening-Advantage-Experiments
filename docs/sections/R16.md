### Stream Overview & Mathematical Target
R16 regenerates v87's most-cited empirical claim across L57, L87, L329, and L374: 80% of dated directional episodes fall out of budget. It dates bull/bear phases of four ETF streams (SPY, PFF, VNQ, BWX) over 2000-2025 using Pagan-Sossounov and Lunde-Timmermann algorithms, then prices two detection floors on every phase: ADD_min_unc = 504 ln(gamma)/SR^2 (Sharpe ceiling) and ADD_min_sign = ln(gamma)/kl(q_phase || q_ref) (Bernoulli budget). Three arms are materialized: canonical (66 phases, substitution on SPY), strict_ps (48 phases, pure Pagan-Sossounov), and symmetric (102 phases, consistent substitution).

### Execution & Verification
Execution enforces strict single-threaded determinism via `enforce_strict_determinism()` and `PYTHONHASHSEED=42` with BLAS pins. No stochastic surface exists: all outputs are deterministic functions of the four FirstRate price series. Control C8 asserts byte-identity of 7 carried primitives. The pipeline verifies partition integrity (C1), degeneracy handling (C5), published count reconstruction (C2), step-of-one behavior (C3), and boundary convention sensitivity (C4). All controls pass deterministically with trigger probability zero under a correct implementation.

### Output Artifacts & Deviation Classification
Artifacts include `R16_regime_census.csv` (66 rows), `R16_regime_census_strict_ps.csv` (48 rows), `R16_regime_census_symmetric.csv` (102 rows), `R16_sign_floor.csv` (66 rows), `R16_feasibility_vs_gamma.csv` (6 rows), `R16_boundary_convention_delta.csv` (66 rows), and `R16_claims.tex` with 32 LaTeX macro definitions under the RSixteen prefix. Phase count reproduces exactly (D0). Out-of-budget fraction is D1 (80.3% rounds to 80%). Floor fraction envelope lower bound is D2 (50.1% vs 55%). Dating description is D3 (strict PS yields 48, not 66). Sharpe-one cost at gamma=252 is D2 (2786.83 vs 2790). COVID floor at gamma=252 is D2 (34.12 vs 34).
