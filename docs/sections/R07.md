## R07 — Whitening Under Estimated Conditional Mean

### Stream Overview & Mathematical Target
R07 evaluates the whitening property when the conditional mean is estimated rather than known, deploying a 7×4 grid of AR(1)-GARCH(1,1) streams with φ ∈ {0, 0.02, 0.05, 0.075, 0.1, 0.125, 0.15} and rolling-OLS window lengths n ∈ {125, 250, 500, 1000}. The pipeline compares NAIVE (zero-bias), ORACLE (perfect specification), and four OLS arms to quantify residual vulnerability. Threshold λ* = 11.4 is fixed by L241's rule (nearest attainable level at or below nominal) via exact lattice enumeration.

### Execution & Verification
The pipeline enforces strict S7 determinism with single-threaded BLAS, MKL_CBWR=COMPATIBLE, and PYTHONHASHSEED=42. All 28 tests pass: C5 source identity validates 7 carried primitives byte-for-byte against the witness, C1 operator identity confirms unified ast.Gt exceedance, and calibration checks verify non-anticipative OLS formulations. Naive FPR at φ = 0.15 measures 21.0% (v87: 20.8%, D1), lattice bounds 4.34%-5.10% (v87: 4.29%-5.03%, D1), bias 3.1×10⁻³ (v87 bound: 2.9×10⁻³, D1).

### Output Artifacts
Generated artifacts include 6 CSV files (estmean_lb_fpr, estmean_diagnostics, design_effect, eta_scaling, lattice_exact_law, eta_scaling_counterfactual), 1 PNG figure (fig07_estimated_mean.png), and R07_claims.tex with 11 LaTeX macros. D1 deviation documented in DEVIATIONS.md: printed precision shifts in lattice levels, naive FPR, and bias bound due to cryptographic re-keying, with all qualitative claims preserved.

