## R09 — Anytime-Valid Detection on the Fair-Coin Stream

### Stream Overview & Mathematical Target
R09 reproduces v87 Figure 9 and L243, validating anytime-valid inference via Ville's inequality. A sign-CUSUM calibrated to 5% at H = 5,000 is monitored continuously to 4H = 20,000, where its false-alarm rate climbs to 19.88%. A 96-component mixture martingale maintains P(sup_t E_t >= 1/alpha) <= alpha, ensuring time-uniform control. e-CUSUM satisfies ARL0 >= 1/alpha.

### Execution & Verification
Strict S7 determinism is enforced with single-threaded BLAS, MKL_CBWR=COMPATIBLE, PYTHONHASHSEED=42, and structural common random numbers via y_t = (rng.random(size) < p). All 31 tests pass. C5 source identity validates wilson_ci byte-for-byte. C3 martingale bound uses one-sided Kolmogorov (p = 0.9975, not fired). C4 Spearman positive control with exact permutation null shows no gates met. Family-wise error rate 3.94% < 5% ceiling.

### Output Artifacts
Generated artifacts: 5 CSV files (R09_validity_stopping, R09_eprocess_race, R09_level_granularity, R09_arl0, R09_eprocess_race_control_ecusum), 1 PNG figure (fig09_anytime_valid.png), R09_claims.tex with 7 LaTeX macros. D1-D2 deviations documented in DEVIATIONS.md: CUSUM peeking FPR 18%->19.9%, MIX ADD 409->410, CUSUM ADD 539->533, all qualitative claims preserved.

