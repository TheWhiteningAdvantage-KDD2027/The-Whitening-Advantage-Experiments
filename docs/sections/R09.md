### Stream Overview & Theoretical Target

R09 validates anytime-valid detection on the fair-coin stream (v87 Figure 9, L243). It demonstrates that a sign-CUSUM calibrated to 5% at horizon H=5000 sees its false-alarm rate climb to ~20% under peeking to 4H, while a 96-component mixture martingale (16 start times × 3 betting fractions × 2 sides) maintains time-uniform control at or below α across all seven levels. e-CUSUM provides a third arm satisfying ARL0 ≥ 1/α. The stream establishes Ville's inequality on non-negative martingales as the foundation for anytime-valid inference.

### Execution & Methodology

The campaign executes under strict determinism (PYTHONHASHSEED=42, BLAS/CBWR pins) with 128-bit re-keying: M1 certifies the mixture kernel, calibration derives λ* from 50 000 streams, H0 runs 20 000 streams with three arms on shared randomness, H1 evaluates 2 000 streams across 10 drifts. Six controls ensure reproducibility: C1 (censoring-ARL0 linkage), C2 (computed bound flag), C3 (Kolmogorov D+ gate at 0.01), C4 (Spearman monotonicity gate at 0.01), plus calibration coherence and cross-check. All 31 pytest assertions pass.

### Key Findings & Deviations

All qualitative claims hold: mixture bounded by α under peeking (ratios 0.948-1.000), CUSUM exceeds mixture at all levels (22.9-68.5 paired SEs), e-CUSUM clears ARL0 ≥ 1/α with minimum margin 20.54×. Three numerals move at printed precision under re-keying: peeking FPR 18%→20% (D2), MIX ADD 409→410 (D2), CUSUM ADD 539→533 (D2). Panel C's CUSUM/MIX curves are horizon artefacts at 65-99% censoring; Panel B's ADD is conditional on detection. Register entries: R09-campaign-redraw (D2), R09-arl0-censoring, R09-add-conditioning, R09-stream-counts.
