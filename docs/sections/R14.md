### Stream Overview & Scientific Objective
R14 reproduces Figure 16 and L345 of v87: an iso-FPR race between the recentred sign-CUSUM (Concept) and a CUSUM on honestly standardized GARCH residuals (Eco-L1) on daily Bitcoin and Ethereum returns, plus quasi-Gaussian t₃₀ synthetic controls. The experiment corroborates the efficiency reversal claim across 11 drift magnitudes with both arms calibrated to a common realized false-alarm rate on real placebo windows.

### Execution & Deterministic Controls
Execution enforces strict determinism via `enforce_strict_determinism()` and 128-bit SeedSequence re-keying. The pipeline runs both migrated and `--legacy-seeds` diagnostic arms. All Bitcoin quantities reproduce bit-identically: iso-FPR 4.7% (5/106), 106 onsets, ν̂ = 2.78, delay ratios 0.74 at c = 0.35 and 1.01 at c = 1.5, mean 0.87. The synthetic control numerals move (0.98–1.14, mean 1.06 → 0.95–1.24, mean 1.04) due to entropy migration, registered as R14-campaign-redraw (Class A, D2).

### Output Artifacts & Deviation Classification
Artifacts include `R14_crypto_diagnostics.csv`, `R14_crypto_isofpr_race.csv` (88 rows), `R14_onset_delays.csv` (7832 rows), `R14_qmle_recovery.csv`, figure `fig16_crypto_race.png`, and LaTeX macros `R14_claims.tex` with 17 \newcommand definitions under the \RFourteen prefix. Ten of eleven v87 numerals classify D0; the three synthetic control numerals classify D2 with qualitative claims preserved via paired bootstrap interval [0.9793, 1.0688] covering the published 1.06.
