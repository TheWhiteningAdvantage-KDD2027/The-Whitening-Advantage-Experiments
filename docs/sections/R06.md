### Stream Overview & Mathematical Target
R06 maps the empirical validity boundaries of Proposition prop:whitening, which states that the binary error stream of a non-anticipative classifier predicting the sign of a return is exactly i.i.d. Bernoulli(1/2) regardless of GARCH dynamics. Panel (A) validates that the binary stream stays white across a 13-point Gamma grid (1–200) despite losing E[eps^4] beyond Gamma ≈ 41.58. Panel (B) establishes sharp task boundaries: non-median thresholds (c ≥ 0.5) and continuous MSE loss re-inherit autocorrelation with 100% rejection.

### Execution & Verification
The experiment is a verified port: six primitives (wilson_ci, gamma_exact, solve_beta_for_gamma, generate_garch, evaluate_sign_task, evaluate_continuous_loss) are byte-identical to the witness, ensuring byte-identical CSVs. Execution spans 1,300 primary streams (13 Gamma × 100 seeds) plus a 1,300-stream counterfactual arm with independent per-cell seeds to measure the paired design effect (Kish deff = 3.21, effective N = 405). All 16 pytest assertions pass, including pooled binary rejection at 4.77% with cluster-robust 95% interval [2.92%, 6.92%] covering nominal 5%, and saturated task boundaries at 100%.

### Output Artifacts
Artifacts include `R06_gamma_grid.csv` (1,300 rows, byte-identical to witness), `R06_task_boundary.csv` (500 rows, byte-identical), `R06_gamma_grid_independent_seeds.csv` (1,300 rows, counterfactual arm), figure `fig06_validity_map.png`, and LaTeX macros `R06_claims.tex` defining Gamma boundaries, rejection rates, design effect, and Wilson confidence intervals.
