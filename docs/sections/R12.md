# R12 — Volatility misspecification and moment singularity

R12 measures `articleB_whitening_v87.tex` **Figure 12** (`fig:leverage`, tex L585) and **Figure 13**
(`fig:fat_tails`, tex L592) and the two paragraphs they serve, L349 and L353. Two experiments:

- **A — asymmetric misspecification.** GJR-GARCH streams with leverage `γ_lev ∈ {0.00, 0.02, …, 0.28}`
  (15 points), **10 000 streams per point**, `n = 7 000` steps, pseudo-Gaussian innovations
  (`ν = 100`) that isolate leverage from tail effects. The econometric baseline standardises with the
  **symmetric** GARCH(1,1) filter `α_sym = α + γ_lev/2`; the Concept pipeline monitors the sign
  stream at a fixed threshold.
- **B — moment singularity.** GARCH(1,1) streams with standardized Student-`t` innovations on
  `ν ∈ {10, 9, 8, 7.5, 7, 6.5, 6, 5.5, 5, 4.75, 4.5, 4.25, 4.2, 4.1, 4.05, 4.01}` (16 points),
  **1 000 streams per point**, `n = 10 000`, drift `c = 1.0` σ.

**Both paragraphs reproduce qualitatively in full, and there is no D3.** The baseline's Ljung–Box
rejection climbs `5.4 % → 24.2 %` and its false-alarm rate `3.5 % → 20.5 %` across the leverage grid
while the sign pipeline holds `7.4–8.5 %`; detection decays monotonically over the reliable domain,
collapses below the 50 % censoring threshold at exactly `ν ≤ 5.5`, and the Concept delay stays flat
at `34–38` steps. Ten of twenty classified numerals move at v87's own printing precision under the
mandated re-keying (**D2**, `R12-campaign-redraw`); six are D1 and four are structural D0.

**One structural fact is not in either caption and it dictates the design of this experiment.**
`simulate_gjr_garch` draws the whole innovation vector *before* the variance recursion, so
`ε_t = √σ²_t · z_t` with `σ²_t > 0` and therefore `sign(ε_t) = sign(z_t)` **exactly**. Experiment A
holds `ν` and `n` fixed across the grid, so under the mandated key — role and index only — the
Concept sign stream is **bit-identical at all fifteen `γ_lev`**. Published on that arm, "leverage-
invariant" would be true *mechanically* rather than measured. R12 therefore runs two Concept arms and
publishes only the one whose key breaks the pairing; see "The two Concept arms" below.

## Execution

```bash
./run_experiment_R12.sh
./run_experiment_R12.sh --n-jobs 6         # byte-identical artefacts (control C7, axis 2)
pytest tests/test_R12_claims.py -v
```

The submitted campaign ran 166 000 streams; this port runs **316 000** — the second Experiment A pass 
is what the two-arm design costs.

Every stream of this stream — campaign stream, identity witness, clamp subsample, bootstrap draw — is
keyed on its **role and index alone** through a 128-bit `SeedSequence`, and `NUM_CHUNKS_A = 25`,
`NUM_CHUNKS_B = 10`, `NUM_CHUNKS_CLAMP = 4` fix the chunk decomposition, so `--n-jobs` cannot move a
value. `run_all.sh` discovers `run_experiment_R12.sh` by sorted enumeration; neither `run_all.sh` nor
`run_tests.sh` is edited by this experiment.

## Expected artefacts

**Two CSVs certify v87; two certify controls.** An artefact evaluator opening
`results/R12_gjr_student/data/` cannot otherwise tell a manuscript-facing file from a control
artefact, so the partition is stated explicitly:

| path                                                       | certifies                                                                                                                       |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| `…/data/R12_leverage_fpr.csv`                              | **v87.** 15 rows ← `protocol_expA_leverage_fpr.csv`, plus Wilson bounds, the arm of every column, and the 30 binomial p-values of C5 |
| `…/data/R12_singularity_add.csv`                           | **v87.** 16 rows ← `protocol_expB_singularity_add.csv`, plus detection counts, Wilson bounds and **`SEM_Data_Raw`** (control C2)  |
| `…/data/R12_concept_crn_witness.csv`                       | **no published value** — control C8: the degenerate CRN Concept arm, kept as an identity witness                                  |
| `…/data/R12_diagnostics.csv`                               | **no published value** — controls C4, C5, C8, C9, C10 and the extremum envelopes, 245 rows in long format                        |
| `…/figures/fig12_leverage.png`                             | v87 Figure 12 (`fig:leverage`)                                                                                                   |
| `…/figures/fig13_fat_tails.png`                            | v87 Figure 13 (`fig:fat_tails`)                                                                                                  |
| `…/tables/R12_claims.tex`                                  | 21 `\newcommand` macros, cardinal prefix `\RTwelve`                                                                              |
| `logs/R12_gjr_student/exp_R12_gjr_student.log`             | full run log, the witness source of the six routines the port adapts or supersedes, and every artefact's SHA-256                  |

## The two Concept arms

**The CRN arm is degenerate, and control C8 asserts it rather than remarking it.** On 50 subsampled
seeds the SHA-256 of the monitored binary stream `(ε[2000:7000] > 0)` is identical at all fifteen
`γ_lev` — 750 pairs, exactly 50 distinct digests, one per seed — and the run exits `1` otherwise, so
a later change to `simulate_gjr_garch` cannot silently break it. The consequences are measured:

| arm                                       | key                                      | `deff(fp_concept)` | `deff(fp_data)` | `n_eff` |
| ----------------------------------------- | ---------------------------------------- | ------------------ | --------------- | ------- |
| `expA` (identity witness)                 | `("R12", "expA", s)`                     | **15.0015**        | 9.1059          | 10 000  |
| `expA_concept_indep` (**published**)      | `("R12", "expA_concept_indep", g_i, s)`  | **1.0004**         | 0.9528          | 150 000 |

A design effect of exactly 15 on a 15-point grid is the arithmetic signature of readings that are one
number repeated. `R12_concept_crn_witness.csv` carries that one number fifteen times, its
`supports_published_claim` column is `False` on every row, and **no macro, figure point or interval
reads it**. The Data arm is *not* degenerate on either key — the symmetric filter reads `ε²_{t-1}`
through the variance recursion, which carries `γ_lev` — so the pairing sharpens the Data comparison
(`deff = 9.11`) instead of collapsing it. The key carries the grid **index**, an integer, never the
float grid value, so no key can depend on a platform's decimal formatting.

**This is the same finding `R11-regenerated` records on R11's H0 Concept arm and entry 17 records on
R06's error streams.** An undeclared paired design is a defect of analysis rather than of experiment;
R12 declares it, prices it, and publishes off the arm that breaks it.

## The measurement

| `γ_lev` | `fpr_data` | `fpr_concept` | `lb_data_pct` | `lb_concept_pct` |
| ------- | ---------- | ------------- | ------------- | ---------------- |
| `0.00`  | `3.46 %`   | `8.06 %`      | `5.41 %`      | `5.21 %`         |
| `0.06`  | `4.76 %`   | `8.07 %`      | `5.81 %`      | `5.19 %`         |
| `0.14`  | `10.28 %`  | `8.47 %`      | `7.71 %`      | `5.16 %`         |
| `0.20`  | `14.81 %`  | `7.83 %`      | `11.66 %`     | `5.25 %`         |
| `0.28`  | `20.48 %`  | `7.38 %`      | `24.19 %`     | `5.17 %`         |

The baseline crosses the 5 % nominal at `γ_lev = 0.08` and reaches `5.92 ×` its leverage-free rate at
the last grid point. **The grid runs to the edge of the stationary region**: under innovations
symmetric about zero `E[1{ε<0}] = 1/2`, so the GJR second-moment condition is
`α + γ_lev/2 + β < 1`, which at `γ_lev = 0.28` is `0.99`. Every unconditional quantity at that point
is estimated on a process whose autocovariances decay at `0.99^h`.

| `ν`     | `det_rate_data` | `ADD_Data` | `SEM_Data_Raw` | `ADD_Concept` |
| ------- | --------------- | ---------- | -------------- | ------------- |
| `10`    | `0.825`         | `1774.27`  | `52.67`        | `37.900`      |
| `7`     | `0.621`         | `2158.35`  | `69.17`        | `36.709`      |
| `6`     | `0.518`         | `2434.61`  | `84.77`        | `36.055`      |
| `5.5`   | `0.471`         | censored   | `93.57`        | `35.688`      |
| `4.25`  | `0.341`         | censored   | `112.64`       | `34.005`      |
| `4.01`  | `0.345`         | censored   | `113.45`       | `33.638`      |

**`SEM_Data_Raw` is this port's addition and it is not decoration.** The witness leaves both
`ADD_Data` and `SEM_Data` `NaN` below a 50 % detection rate while writing `ADD_Data_Raw`, so the
dispersion is missing on exactly the rows v87 publishes: L353's `2,400–3,000` is explicitly scoped to
the censored domain ("the surviving minority carries survivorship-biased delays"). Without a standard
error there, §S3's rule — a printed bound is breached at D3 only if the regenerated 95 % interval
excludes it — cannot be evaluated at all.

**The fourth-moment boundary is derived, and the distance to the collapse is reported without a
mechanism.** `E[ε⁴] < ∞` iff `α² · 3(ν−2)/(ν−4) + 2αβ + β² < 1`, giving `ν* = 4.0811` at
`(α, β) = (0.05, 0.85)`. **Only `ν = 4.05` and `ν = 4.01` sit beyond it**, while the detection
collapse begins near `ν = 5.5–6`. §S4.5 forbids attributing a mechanism to that distance, and
`docs/DEVIATIONS.md` `R02c-mechanism-constraints` already records the identical shape on the i.i.d.
arm — infinite eighth moment up to `ν ≤ 8`, effect already gone at `ν = 7` — under the same "mechanism
not identified" ruling. R12 cross-references it and invents nothing.

**The Data threshold carries the exact penalty and the Concept threshold does not.**
`compute_gamma_exact(0.05, 0.85) = 2.2208`, so Experiment B's Data CUSUM runs at
`λ_iid · Γ = 65 × 2.2208 = 144.35` while the Concept CUSUM runs at `λ_C = 10` with no multiplier.
That asymmetry is what Figure 13 exists to show.

## Controls

| control | what it establishes                                                                                             | trigger probability under its own H₀ |
| ------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| **C1**  | `det_rate_concept` is a computed `len(…)/n_seeds` and not a literal, asserted on the script's own AST             | **0** — structural; failure is a D3  |
| **C2**  | the censoring rule, logged **before** the regenerated frame is read; `SEM_Data_Raw` on every cell                | **0** — deterministic                |
| **C3**  | task boundary **as a read**: R07's `φ = 0.15` cell printed beside the orphan witness, gap unexplained            | deterministic; no macro, no entry    |
| **C4**  | monotone decay of `det_rate_data`, domain pre-declared, paired standard errors everywhere                        | qualitative D3 criterion, not a level test |
| **C5**  | Ljung–Box **calibration**, KS of the 30 binomial p-values against `Uniform(0,1)`                                 | reported, gates nothing (`1 − 0.95³⁰ = 78.5 %`) |
| **C6**  | `ast` source identity, three layers: 3 primitives byte-identical (1 160 chars), 6 routines quoted, **29 statements** verbatim | **0** unless a copy has drifted |
| **C7**  | two consecutive runs plus one different-`--n-jobs` run, SHA-256 identical on all seven artefacts                 | **0** — deterministic                |
| **C8**  | the CRN identity, **asserted** with `sys.exit(1)`; Kish design effect on both arms                              | **0** — structural                   |
| **C9**  | leverage invariance as an OLS **slope** with a seed-cluster bootstrap null                                       | exactly `0.01`, two-sided            |
| **C10** | the DGP variance clamp, measured on 6 200 streams by an instrumented copy asserted bit-identical                 | reported, gates nothing              |
| **C11** | legacy-global inertness: each worker returns bit-identical output under two different global RNG states          | **0** — deterministic                |

**Results.** C1: the single producing site is `len(d_concept) / n_seeds`, and `det_rate_concept` is
`1.0000` at all sixteen `ν` **because `n_detected_concept == n_seeds` row by row** — the column is a
real 1 000/1 000, not a literal. C4: all six adjacent pairs of the uncensored domain decrease, at
`+0.044` to `+0.062` with paired standard errors of `0.0085`–`0.0104`; one censored inversion,
`ν 4.2 → 4.1` at `−0.0060`, `z = −0.74`, 95 % `[−0.0219, +0.0099]`, characterised and never corrected.
C5: Concept arm `D = 0.1701`, `p = 0.7172`; Data arm `D = 0.9369`, `p = 2.0 × 10⁻¹⁸`. C6: 1 160
characters compared, 0 drift, 29 statements found verbatim. C9: slope `−0.9286` points per unit
`γ_lev`, analytic OLS SE `0.8797`, seed-cluster bootstrap SE `0.8034` (ratio `0.9132`), 95 %
`[−2.4500, +0.6577]`, `p = 0.2477` — the gate does not fire, and over the whole grid the fitted drift
is `0.26` points. C10: **0 clamped steps of 52 993 800**, and the instrumented copy returns a
bit-identical `ε` on all 6 200 streams. C11: both workers bit-identical under two global states.

**Why the Data arm's KS rejection is not a failure, stated in the same breath as the number.** The
Data arm's Ljung–Box rate climbing to `24.19 %` **is** v87 L349's printed claim, so a uniformity test
on that arm rejects precisely because the manuscript is right. That direction is a positive control on
the instrument's power, not a calibration reading; the Concept arm's 15 p-values are the ones a reader
should weigh, and nothing is gated on either. The family-wise arithmetic — `1 − 0.95³⁰ = 78.5 %` — is
computed and logged before any p-value is interpreted, and it is what forbids a binary reading.

**Every range v87 prints is an extremum over a grid, and each ships with its own null law** (§S4bis,
fourth corollary). Seed bootstrap, 2 000 replicates:

| macro                             | point value | bootstrap 95 %          |
| --------------------------------- | ----------- | ----------------------- |
| `\RTwelveFprConceptMin` / `Max`   | `7.38 %` / `8.47 %`   | `[6.85, 7.59] %` / `[8.30, 9.07] %` |
| `\RTwelveLbConceptMin` / `Max`    | `4.65 %` / `5.37 %`   | `[4.18, 4.73] %` / `[5.31, 5.87] %` |
| `\RTwelveAddConceptMin` / `Max`   | `33.638` / `37.900`   | `[33.23, 34.07]` / `[37.34, 38.50]` |
| `\RTwelveAddDataCensoredMin` / `Max` | `2610.23` / `2998.77` | `[2432.3, 2778.5]` / `[2854.8, 3249.7]` |

They are **descriptive and gate nothing**. The gate is control C9's slope, which has a distribution;
a max minus a min over 15 noisy estimates grows with the grid size at a fixed underlying variance and
can neither accept nor reject invariance.

## Known deviations from the submitted manuscript

### D2 — both campaigns are redrawn (`R12-campaign-redraw`)

Preamble §S6 requires a 128-bit `SeedSequence` keyed on the role and index alone. The delivered script
keys each stream on the **process parameter itself** — `int(gamma_lev * 1000) + s * 17` at l.131 and
`int(nu * 100) + s * 23` at l.194 — so the migration is required by the specification and not by any
observed failure, and it redraws all 166 000 published streams. **Ten printed numerals move at v87's
own printing precision:**

| v87 site                          | printed   | regenerated  | witness  | severity |
| --------------------------------- | --------- | ------------ | -------- | -------- |
| L349 Ljung–Box at `γ_lev = 0`     | `5.1 %`   | **`5.4 %`**  | `5.1 %`  | **D2**   |
| L349 Ljung–Box at `γ_lev = 0.28`  | `24.6 %`  | **`24.2 %`** | `24.6 %` | **D2**   |
| L349 FPR at `γ_lev = 0`           | `3.2 %`   | **`3.5 %`**  | `3.2 %`  | **D2**   |
| L349 FPR at `γ_lev = 0.28`        | `20.6 %`  | **`20.5 %`** | `20.6 %` | **D2**   |
| L349 Concept FPR minimum          | `7.6 %`   | **`7.4 %`**  | `7.6 %`  | **D2**   |
| L349 Concept FPR maximum          | `8.4 %`   | **`8.5 %`**  | `8.4 %`  | **D2**   |
| L349 Concept Ljung–Box minimum    | `4.6 %`   | **`4.7 %`**  | `4.6 %`  | **D2**   |
| L353 detection at `ν = 10`        | `83 %`    | **`82 %`**   | `83 %`   | **D2**   |
| L353 detection at `ν = 7`         | `61 %`    | **`62 %`**   | `61 %`   | **D2**   |
| L353 censored delay minimum       | `2,400`   | **`2,600`**  | `2,400`  | **D2**   |
| L349 Concept Ljung–Box maximum    | `5.4 %`   | `5.4 %` (`5.37`) | `5.4 %` | D1     |
| L349 "factor of six"              | `6`       | `6` (`5.92`) | `6`      | D1       |
| L353 collapse threshold           | `5.5`     | `5.5`        | `5.5`    | D1       |
| L353 censored delay maximum       | `3,000`   | `3,000` (`2998.77`) | `3,000` | D1 |
| L353 Concept delay minimum        | `34`      | `34` (`33.638`) | `34`  | D1       |
| L353 Concept delay maximum        | `38`      | `38` (`37.900`) | `38`  | D1       |
| Figure 12 / 13 streams per point  | `10,000` / `1,000` | same | same | **D0**   |
| Figure 12 / 13 grid sizes         | `15` / `16` | same       | same     | **D0**   |

**`83 % → 82 %` deserves one sentence, because it is a knife edge.** The regenerated detection rate at
`ν = 10` is exactly `825/1000`. As a decimal that is `82.5 %`, which would round to `83` under
round-half-up and to `82` under round-half-even — but `0.825` is not representable in binary64 and its
nearest double is `0.82499999999999995559…`, so `82` is what every rounding convention returns and the
D2 is not an artefact of the tie rule. The underlying displacement is `−0.005` against a
two-campaign standard error of `0.0168`, i.e. `0.30 σ`: an ordinary draw.

**Every qualitative claim of L349 and L353 holds.** The baseline explodes and crosses its nominal, the
sign pipeline is flat (C9's slope null is not rejected), detection decays monotonically on the reliable
domain, the collapse threshold is exactly `ν ≤ 5.5`, and the Concept delay stays at `34–38` steps.
Pre-classified Class A / D2 **before the first run** by the `R03-campaign-redraw`,
`R05-campaign-redraw`, `R11-regenerated`, `R13-campaign-redraw`, `R07-campaign-redraw`,
`R09-campaign-redraw` and `R10-campaign-redraw` precedents. No parameter, tolerance, seed or bound was
moved.

### The halt candidate that did not fire

v87 L353 prints `2,400`–`3,000` **rounded to the hundreds**, and the submitted witness's own `2443.18`
and `3005.28` both round onto those numerals — so `3005` was never a contradiction and the watch item
is the **rounding bracket `[2350, 3050)`**, not the numerals. Regenerated: minimum `2610.23` at
`ν = 5.5` (`SEM_Data_Raw = 93.57` on 471 surviving streams, 95 % lower bound `2426.85`), maximum
`2998.77` at `ν = 4.25` (`SEM_Data_Raw = 112.64` on 341 surviving streams). Both stay inside the
bracket at the 95 % level, so §S3's D3 criterion is not met and the pair is a D2 at its lower end
only.

### Class A, no severity — the CRN degeneracy (`R12-concept-crn-degeneracy`)

Declared in full above. No published value is affected, because no published value is taken from the
degenerate arm.

## Reported and not registered

**The two orphan CSVs have no producing script.** `expA_argarch_boundary.csv` and
`expB_race_condition.csv` were delivered with the attachment set; a grep of the whole delivery for
`argarch_boundary|race_condition` returns only the prompt. They are vendored verbatim under
`data/reference/R12/orphans/` with a README stating the grounds. **No register entry and no
camera-ready candidate is opened on either.**

- Control C3 **reads** `expA_argarch_boundary.csv` and recovers its arithmetic exactly: both intervals
  are Wilson score intervals at `n = 1000` and `z = Φ⁻¹(0.975) = 1.959963984540054`, reproduced to all
  17 significant digits from the counts `1000/1000` and `45/1000` alone. That recovers the *interval
  construction*, **not** the data-generating process — stream length, innovation law, AR coefficient,
  estimator and lag count all remain unfixed. The orphan reads `1.000 / 0.045` where R07's certified
  campaign reads `0.9979 / 0.0492` at `φ = 0.15` on `N = 10 000`. **The gap is stated and left
  unexplained**: with the design unknown it cannot be attributed, and §S4.5 forbids inventing a
  mechanism. The claim behind the file is v87 L302, which is R07's mission statement and which R07 has
  already delivered; rebuilding it here would put a third pair of numbers behind one printed sentence.
  The two macros the R12 prompt lists for it, `\RTwelveBoundaryNaiveRate` and
  `\RTwelveBoundaryMedianRate`, are **deliberately not emitted**.
- `expB_race_condition.csv` is **produced and not cited**. 1 000 rows; `delay_frozen` populated on all
  of them (min `35`, max `356`, mean `76.149`); `delay_arf` empty on **999 of 1 000**, the single
  populated row being `seed = 492` at `216.0`. Reported as measured; **the mechanism is not
  attributed** — with no producing code a missing value cannot be told from a censored run, an
  unwritten column or an aborted arm. v87 cites no frozen-versus-ARF race at L349, at L353 or in
  either caption, and a camera-ready candidate must attach to a manuscript sentence.

**R12's own negative witnesses are the cells it actually runs**, not the orphan: `γ_lev = 0`, where the
symmetric filter is correctly specified and the baseline holds its level (`3.46 %` FPR, `5.41 %`
Ljung–Box), and `ν = 10`, where the fourth moment is finite and the Data pipeline detects on 82.5 % of
streams. Both are measured, both are in the published CSVs, both are classified against v87.

**The variance clamp never binds.** `simulate_gjr_garch` l.106 caps `σ²_t ≤ 10⁴ · σ²_unc`, which at a
persistence of `0.99` and near `ν = 4` is a silent execution path that could have produced the
published collapse mechanically. Measured on 6 200 streams — 200 per grid point on both experiments,
each asserted to return a bit-identical `ε` to the carried primitive — the clamp binds on **0 of
52 993 800 recursion steps**. The degraded-path hypothesis the epistemic-asymmetry rule requires be
excluded before any D0/D1 is therefore excluded by measurement, not by inspection.

**An open question is posed and not settled.** v87 L349 calls `α_sym = α + γ_lev/2` the symmetric
GARCH(1,1) "*population limit*". What the design gives is **mean-matching**: under a symmetric
innovation law `E[ε²·1{ε<0}] = E[ε²]/2`, so `E[h_t]` obeys the same recursion as `E[σ²_t]` and matches
the unconditional variance exactly. A "population limit" in the QMLE sense is the Gaussian pseudo-true
parameter of [Bollerslev & Wooldridge, 1992], the minimiser of the expected quasi-log-likelihood. The
two need not coincide, and **no measurement in this stream decides which one `α_sym` is**: the witness
never fits anything, it substitutes `α + γ_lev/2` in closed form. Recorded in `docs/audits/AUDIT_R12.md`
and left open.

## Cosmetic divergence from the submitted figures

Preamble §S6 declares this class. Both panel titles are bold and centred (single-panel figures, so no
letter prefix); both axes carry their own sample size; Figure 12's bands are asymmetric **Wilson**
intervals where the delivered script drew symmetric normal half-widths, and its Concept legend entry
names the arm the curve comes from; Figure 13's censored branch carries a `SEM_Data_Raw` band that the
submitted figure cannot draw, because the submitted artefact has no standard error there. No numerical
value moves on any of these accounts.

The submitted PNGs `Fig15_Robustness_Leverage.png` and `Fig16_Robustness_FatTails.png` are **not
present anywhere in the delivery**, so the figure comparison was made against the plotting code of
`Priorite_10_robustness_gjr_student.py` (l.276–298, l.406–451) rather than against the images.
