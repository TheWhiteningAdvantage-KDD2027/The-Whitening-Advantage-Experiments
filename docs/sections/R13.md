## R13 — Oracle Ceiling and the Clairvoyant Frontier

### Stream Overview & Mathematical Target
R13 reproduces v87 Figure 14 and L331, establishing the clairvoyant frontier for concept-drift detection under a parameter oracle: a CUSUM on returns standardized by the conditional volatility of a GARCH(1,1) fitted on a window including the change point, with causal filtration and a frozen-volatility bootstrap null. The experiment validates that a clairvoyant monitor detects the 2020 COVID-19 crash in 3 trading days at a 1.3% phase false-alarm probability (likelihood-ratio increments) to 16 days (standardized-mean CUSUM), with path divergence 10.6 times the unconditional budget via Jensen's inequality. Four SPY episodes are evaluated (2020 crash, 2009 recovery, 2019 advance, 2011 correction) across three volatility oracles (V1: look-ahead GARCH, V2: leave-one-out realized, V3: contaminated realized).

### Execution & Verification
S7 determinism enforced via single-threaded BLAS, MKL_CBWR=COMPATIBLE, PYTHONHASHSEED=42. Campaign redraw classified D2: 128-bit re-seeding of all Monte Carlo components (20,000 bootstrap FPR_H replicates, 5,000 ARL0 replicates) replaces delivered bare integer seeds. All 24 tests pass, including the two self-invalidating assertions: C1 verifies the published delay-FPR pair (3 days, 1.3%) is carried by one row at E1/D2/V1/OP2b_ARL0_252, and the explicit deviation test confirms 1.1% does not round to 1.3% at printed precision. Control C4 verifies frozen volatility path digests match recomputed paths from persisted GARCH parameters. Control C7 asserts byte identity for 6 carried primitives against owning files.

### Output Artifacts
Generated artifacts: 6 CSV files (R13_oracle_frontier, R13_oracle_operating_points, R13_oracle_diagnostics, R13_clairvoyant_floor, R13_detector_recovery, R13_qmle_recovery), Figure 14 PNG (fig14_oracle_frontier), R13_claims.tex with 11 LaTeX macros. D2 deviation documented in DEVIATIONS.md: COVID-19 LR CUSUM FPR_H 1.3%→1.1% at OP2b_ARL0_252; all other numerals (3-day delay, 16-day delay, 10.6x Jensen ratio, census verdicts) preserve qualitative claims at printed precision.
