# R04 — Iso-FPR race and relative efficiency (Figure 4, Table 3)

### Stream Overview & Mathematical Target

R04 races four monitoring pipelines under a location drift, every arm calibrated by bisection to the same 5% false-alarm rate: Recalib ((eps_t / sigma_hat_t)^2 - 1), Eco_L1 (eps_t / sigma_hat_t), Oracle_Eco (standardized by true GARCH parameters), and Concept (binary sign-error stream). Target: 2,000 null streams of 5,000 steps, 500-step warm-up, GARCH(1,1) with alpha = 0.05, beta solved for Gamma in {1.0, 11.58, 50.0, 200.0}, standardized Student-t_30 innovations. The squared sensor is structurally slow under location drift (second-order entry), and the delay ratio between sign filter and parametric route is governed by Pitman efficiency, which inverts in heavy tails.

### Execution & Verification

The pipeline executes via ProcessPoolExecutor with worker-side seeding (128-bit MD5 condensate of semantic coordinates), ensuring outputs are invariant to worker count. All tests pass (27/27): cardinalities match v87 specification, bisection calibration converges within tolerance, Concept threshold is flat in Gamma (homogeneity p = 0.26), Recalib blind zone persists at lowest Gamma, efficiency ratio is monotone in nu, and Gaussian ceiling is respected. Single-threaded determinism enforced via enforce_strict_determinism(), verified with byte-identical artefacts across different worker counts.

### Output Artifacts

Generated artefacts: R04_bernoulli_constant.csv, R04_isofpr_calibration.csv (16 rows), R04_isofpr_race.csv (64 rows for Figure 4(A) and Table 3), R04_relative_efficiency.csv (6 rows for Figure 4(B)), R04_cusum_vs_adwin.csv (8 rows), fig04_isofpr_race.png, tab03_isofpr_race.tex, and R04_claims.tex (37 macros). LaTeX macros define \RFourNullStreams through \RFourAdwinAttainableFpr. Known D3 deviation: submitted Gamma grid collapsed to single point due to transposed argument; counterfactual reproduces published figures when beta pinned to 0.
